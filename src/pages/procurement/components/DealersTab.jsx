import { useState, useEffect, useCallback } from 'react';import Modal from '../../../components/common/Modal';import { dealerApi } from '../../../api/dealerApi';import { MdSearch, MdVisibility, MdPerson, MdPhone, MdLocationOn, MdBusiness } from 'react-icons/md';export default function DealersTab() {  const [dealers, setDealers] = useState([]);  const [loading, setLoading] = useState(false);  const [error, setError] = useState('');  const [search, setSearch] = useState('');  const [filterStatus, setFilterStatus] = useState('');  const [viewDealer, setViewDealer] = useState(null);  const [successMsg, setSuccessMsg] = useState('');  const fetchDealers = useCallback(async () => {    setLoading(true); setError('');    try {      const params = {};      if (search) params.search = search;      if (filterStatus) params.status = filterStatus;      const res = await dealerApi.getAll(params);      setDealers(res.data || []);    } catch (e) { setError(e.message); }    finally { setLoading(false); }  }, [search, filterStatus]);  useEffect(() => { fetchDealers(); }, [fetchDealers]);  useEffect(() => { const t = setTimeout(fetchDealers, 400); return () => clearTimeout(t); }, [search]);  const openView = (d) => setViewDealer(d);  return (    <>      <style>{`        .dt-toolbar { display:flex; align-items:center; gap:10px; margin-bottom:16px; flex-wrap:wrap; }        .dt-search-wrap { position:relative; flex:1; min-width:160px; }        .dt-search { width:100%; padding:9px 12px 9px 34px; border:1.5px solid #e2e8f0; border-radius:10px; background:#f8fafc; font-size:13px; color:#1e293b; outline:none; font-family:inherit; transition:all .2s; box-sizing:border-box; }        .dt-search:focus { border-color:#ef4444; background:#fff; box-shadow:0 0 0 3px rgba(239,68,68,0.08); }        .dt-search::placeholder { color:#94a3b8; }        .dt-search-icon { position:absolute; left:10px; top:50%; transform:translateY(-50%); color:#94a3b8; pointer-events:none; display:flex; }        .dt-select { padding:9px 12px; border:1.5px solid #e2e8f0; border-radius:10px; background:#f8fafc; font-size:13px; color:#1e293b; outline:none; font-family:inherit; cursor:pointer; min-width:130px; }        .dt-table-wrap { overflow-x:auto; border-radius:12px; border:1px solid #f1f5f9; }        .dt-table { width:100%; border-collapse:collapse; min-width:600px; }        .dt-table thead tr { background:#f8fafc; }        .dt-table th { padding:10px 14px; text-align:left; font-size:10.5px; font-weight:700; color:#94a3b8; text-transform:uppercase; letter-spacing:.7px; border-bottom:1px solid #f1f5f9; white-space:nowrap; }        .dt-table td { padding:11px 14px; font-size:12.5px; color:#1e293b; border-bottom:1px solid #f8fafc; vertical-align:middle; }        .dt-table tbody tr { transition:background .1s; }        .dt-table tbody tr:hover { background:#fef2f2; }        .dt-table tbody tr:last-child td { border-bottom:none; }        .dt-cards { display:none; flex-direction:column; gap:10px; }        @media(max-width:640px) { .dt-table-wrap{display:none;} .dt-cards{display:flex;} }        .dt-card { background:#fff; border-radius:14px; border:1px solid #e8edf2; box-shadow:0 2px 8px rgba(15,23,42,0.05); padding:14px 16px; }        .dt-card-top { display:flex; align-items:flex-start; justify-content:space-between; margin-bottom:10px; }        .dt-card-name { font-size:14px; font-weight:700; color:#0f172a; }        .dt-card-id { font-size:11px; color:#94a3b8; margin-top:2px; font-family:monospace; }        .dt-card-meta { display:grid; grid-template-columns:1fr 1fr; gap:6px 12px; margin-bottom:12px; }        .dt-card-meta-item { display:flex; align-items:center; gap:5px; font-size:12px; color:#475569; }        .dt-card-actions { display:flex; gap:8px; }      `}</style>      {/* Toolbar */}      <div className="dt-toolbar">        <div className="dt-search-wrap">          <span className="dt-search-icon"><MdSearch size={15} /></span>          <input className="dt-search" placeholder="Search dealers…" value={search} onChange={e => setSearch(e.target.value)} />        </div>        <select className="dt-select" value={filterStatus} onChange={e => setFilterStatus(e.target.value)}>          <option value="">All Status</option>          <option>Active</option><option>Inactive</option>        </select>      </div>      {/* Alerts */}      {error && <div style={{ padding:'10px 14px', background:'#fef2f2', border:'1px solid #fecaca', borderRadius:10, color:'#ef4444', fontSize:13, marginBottom:12 }}>❌ {error}</div>}      {successMsg && <div style={{ padding:'10px 14px', background:'#ecfdf5', border:'1px solid #a7f3d0', borderRadius:10, color:'#047857', fontSize:13, marginBottom:12 }}>{successMsg}</div>}      {loading && <div style={{ padding:'32px 0', textAlign:'center', color:'#94a3b8', fontSize:13 }}>Loading dealers…</div>}      {/* Desktop Table */}      {!loading && dealers.length > 0 && (        <div className="dt-table-wrap">          <table className="dt-table">            <thead><tr>              <th>Dealer Code</th><th>Name</th><th>Business Name</th><th>Mobile</th>              <th>City</th><th>Status</th><th>Actions</th>            </tr></thead>            <tbody>              {dealers.map(d => {                return (                  <tr key={d.id || d._id}>                    <td style={{ fontFamily:'monospace', fontSize:11, fontWeight:600, color:'#64748b' }}>{d.dealerCode}</td>                    <td style={{ fontWeight:500 }}>{d.name}</td>                    <td>{d.businessName || '—'}</td>                    <td>{d.mobile}</td>                    <td>{d.city || '—'}</td>                    <td><span style={{ display:'inline-block', padding:'3px 10px', borderRadius:12, fontSize:11, fontWeight:600, background:d.isActive ? '#ecfdf5' : '#f3f4f6', color:d.isActive ? '#047857' : '#6b7280' }}>{d.isActive ? 'Active' : 'Inactive'}</span></td>                    <td style={{ display:'flex', gap:6 }}>                      <button onClick={() => openView(d)} style={{ padding:'4px 10px', borderRadius:6, background:'#f8fafc', border:'1px solid #e2e8f0', color:'#475569', cursor:'pointer', fontSize:12, fontWeight:500, fontFamily:'inherit' }}>View</button>                    </td>                  </tr>                );              })}            </tbody>          </table>        </div>      )}      {/* Mobile Cards */}      {!loading && dealers.length > 0 && (        <div className="dt-cards">          {dealers.map(d => {            return (              <div key={d.id || d._id} className="dt-card">                <div className="dt-card-top">                  <div>                    <div className="dt-card-name">{d.name}</div>                    <div className="dt-card-id">{d.dealerCode}</div>                  </div>                  <span style={{ display:'inline-block', padding:'3px 10px', borderRadius:12, fontSize:11, fontWeight:600, background:d.isActive ? '#ecfdf5' : '#f3f4f6', color:d.isActive ? '#047857' : '#6b7280' }}>{d.isActive ? 'Active' : 'Inactive'}</span>                </div>                <div className="dt-card-meta">                  <div className="dt-card-meta-item"><MdPhone size={14}/>{d.mobile}</div>                  <div className="dt-card-meta-item"><MdLocationOn size={14}/>{d.city || '—'}</div>                  <div className="dt-card-meta-item"><MdBusiness size={14}/>{d.businessName || '—'}</div>                  <div className="dt-card-meta-item"><MdPerson size={14}/>{d.contactPerson || d.name}</div>                </div>                <div className="dt-card-actions">                  <button onClick={() => openView(d)} style={{ flex:1, padding:'8px 0', borderRadius:8, fontSize:12, fontWeight:600, cursor:'pointer', fontFamily:'inherit', background:'#f8fafc', color:'#475569', border:'1px solid #e2e8f0' }}><MdVisibility size={14}/> View</button>                </div>              </div>            );          })}        </div>      )}      {/* Empty State */}      {!loading && dealers.length === 0 && (        <div style={{ padding:'48px 24px', textAlign:'center', background:'#f8fafc', borderRadius:12, border:'1px dashed #e2e8f0' }}>          <MdPerson size={40} style={{ color:'#cbd5e1', marginBottom:12 }} />          <div style={{ fontSize:14, fontWeight:600, color:'#475569', marginBottom:4 }}>No dealers found</div>          <div style={{ fontSize:12, color:'#94a3b8' }}>Dealers will appear here once they register via the mobile app</div>        </div>      )}      {/* View Modal */}      {viewDealer && (        <Modal open={!!viewDealer} onClose={() => setViewDealer(null)} title={viewDealer.name} size="lg"          footer={            <div style={{ display:'flex', justifyContent:'space-between', width:'100%', alignItems:'center', gap:12 }}>              <div style={{ fontSize:12, color:'#64748b' }}>Dealer Code: <span style={{ fontWeight:600, color:'#1e293b' }}>{viewDealer.dealerCode}</span></div>              <div style={{ display:'flex', gap:10 }}>                <button onClick={() => setViewDealer(null)} style={{ background:'transparent', border:'1.5px solid #cbd5e1', color:'#475569', padding:'8px 16px', borderRadius:8, fontWeight:600, fontSize:13, cursor:'pointer', fontFamily:'inherit' }}>Close</button>              </div>            </div>          }>          <div style={{ display:'flex', gap:10, marginBottom:16, flexWrap:'wrap' }}>            <span style={{ display:'inline-flex', padding:'4px 10px', background:viewDealer.isActive ? '#ecfdf5' : '#f3f4f6', color:viewDealer.isActive ? '#047857' : '#6b7280', borderRadius:6, fontSize:12, fontWeight:600, border:'1px solid #e2e8f0' }}>{viewDealer.isActive ? 'Active' : 'Inactive'}</span>            {viewDealer.erpClientId && <span style={{ display:'inline-flex', padding:'4px 10px', background:'#dbeafe', color:'#1e40af', borderRadius:6, fontSize:12, fontWeight:600, border:'1px solid #bfdbfe' }}>Synced to ERP</span>}          </div>          <div style={{ display:'grid', gridTemplateColumns:'1fr 1fr 1fr', gap:'14px 20px' }}>            {[              { label:'Contact Person', value: viewDealer.contactPerson || viewDealer.name },              { label:'Mobile',          value: viewDealer.mobile },              { label:'Email',          value: viewDealer.email || '—' },              { label:'City',           value: viewDealer.city || '—' },              { label:'State',          value: viewDealer.state || '—' },              { label:'Pincode',        value: viewDealer.pincode || '—' },              { label:'GSTIN',          value: viewDealer.gstin || '—' },              { label:'PAN Number',     value: viewDealer.panNumber || '—' },              { label:'Credit Limit',   value: viewDealer.creditLimit ? `₹${viewDealer.creditLimit.toLocaleString()}` : '—' },              { label:'Outstanding',    value: viewDealer.outstandingAmount ? `₹${viewDealer.outstandingAmount.toLocaleString()}` : '—' },              { label:'Zone',           value: viewDealer.zone || '—' },            ].map(({ label, value }) => (              <div key={label}>                <div style={{ fontSize:11, fontWeight:500, color:'#64748b', textTransform:'uppercase', letterSpacing:'0.3px' }}>{label}</div>                <div style={{ fontSize:14, color:'#1e293b', marginTop:3 }}>{value}</div>              </div>            ))}            {viewDealer.address && (              <div style={{ gridColumn:'span 3' }}>                <div style={{ fontSize:11, fontWeight:500, color:'#64748b', textTransform:'uppercase', letterSpacing:'0.3px' }}>Address</div>                <div style={{ fontSize:14, color:'#1e293b', marginTop:3 }}>{viewDealer.address}</div>              </div>            )}          </div>        </Modal>      )}    </>  );}